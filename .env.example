# InfiniProxy Client Configuration Example
# Copy this file to .env and replace with your actual values

# ============================================================================
# Proxy Configuration (New Standard)
# ============================================================================
AIAPI_URL=https://aiapi.iiis.co:9443
AIAPI_KEY=your-proxy-api-key-here

# ============================================================================
# Proxy Configuration (Legacy - Backward Compatible)
# ============================================================================
# INFINIPROXY_URL=https://aiapi.iiis.co:9443
# INFINIPROXY_API_KEY=your-proxy-api-key-here

# ============================================================================
# OpenAI SDK Configuration
# Configure the official OpenAI Python SDK to use the proxy
# ============================================================================
OPENAI_BASE_URL=https://aiapi.iiis.co:9443/v1
OPENAI_API_KEY=your-proxy-api-key-here

# ============================================================================
# Anthropic SDK Configuration
# Configure the official Anthropic Python SDK to use the proxy
# ============================================================================
ANTHROPIC_BASE_URL=https://aiapi.iiis.co:9443/v1
ANTHROPIC_API_KEY=your-proxy-api-key-here

# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file to .env in your project root
# 2. Replace 'your-proxy-api-key-here' with your actual API key
# 3. Load environment variables in your application:
#
#    Python (using python-dotenv):
#      from dotenv import load_dotenv
#      load_dotenv()
#
#    Shell (bash/zsh):
#      source .env
#      # or
#      export $(cat .env | xargs)
#
# 4. Use the proxy:
#
#    Direct API calls (Recommended):
#      import requests
#      response = requests.post(
#          f"{os.environ['AIAPI_URL']}/v1/chat/completions",
#          headers={"Authorization": f"Bearer {os.environ['AIAPI_KEY']}"},
#          json={"model": "gpt-3.5-turbo", "messages": [...]}
#      )
#
#    OpenAI SDK:
#      import openai
#      # Environment variables will be automatically loaded
#      client = openai.OpenAI()
#      response = client.chat.completions.create(...)
#
#    Anthropic SDK:
#      import anthropic
#      # Environment variables will be automatically loaded
#      client = anthropic.Anthropic()
#      response = client.messages.create(...)
#
# ============================================================================
# Available Endpoints
# ============================================================================
#
# Chat Completions:
#   - POST /v1/chat/completions (OpenAI-compatible)
#   - POST /v1/messages (Claude/Anthropic-compatible)
#
# Web Scraping & Search:
#   - POST /v1/firecrawl/scrape
#   - POST /v1/firecrawl/search
#   - POST /v1/tavily/search
#   - GET  /v1/serpapi/search
#
# Text-to-Speech:
#   - POST /v1/elevenlabs/text-to-speech
#   - WS   /v1/elevenlabs/text-to-speech/websocket
#
# Speech-to-Text:
#   - POST /v1/elevenlabs/speech-to-text
#   - WS   /v1/elevenlabs/speech-to-text/websocket
#
# Health Check:
#   - GET  /health
#
# ============================================================================
