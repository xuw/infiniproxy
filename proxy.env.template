# InfiniProxy Client Configuration Template
#
# This template configures API clients to use InfiniProxy instead of original endpoints.
# Copy this file to .env or proxy.env and update the values as needed.
#
# Usage with docker-compose:
#   docker-compose --env-file proxy.env up
#
# Usage with direnv:
#   cp proxy.env.template .envrc
#   direnv allow
#
# Usage in shell:
#   export $(cat proxy.env | xargs)

# ============================================================================
# Proxy Configuration (use either naming convention)
# ============================================================================
# Change these values to match your proxy deployment
# Recommended: Use AIAPI_* variables
AIAPI_URL=http://localhost:8000
AIAPI_KEY=your-proxy-api-key-here

# Or use INFINIPROXY_* variables (backward compatible)
# INFINIPROXY_URL=http://localhost:8000
# INFINIPROXY_API_KEY=your-proxy-api-key-here

# For production, use your domain:
# AIAPI_URL=https://proxy.yourdomain.com
# AIAPI_KEY=your-production-api-key

# ============================================================================
# OpenAI Configuration
# ============================================================================
OPENAI_API_BASE=${AIAPI_URL}/v1
OPENAI_BASE_URL=${AIAPI_URL}/v1
OPENAI_API_KEY=${AIAPI_KEY}

# ============================================================================
# Anthropic/Claude Configuration
# ============================================================================
ANTHROPIC_BASE_URL=${AIAPI_URL}/v1
ANTHROPIC_API_URL=${AIAPI_URL}/v1
ANTHROPIC_API_KEY=${AIAPI_KEY}

# ============================================================================
# Firecrawl Configuration
# ============================================================================
FIRECRAWL_BASE_URL=${AIAPI_URL}/v1/firecrawl
FIRECRAWL_API_URL=${AIAPI_URL}/v1/firecrawl
FIRECRAWL_API_KEY=${AIAPI_KEY}

# ============================================================================
# ElevenLabs Configuration
# ============================================================================
ELEVENLABS_API_BASE=${AIAPI_URL}/v1/elevenlabs
ELEVENLABS_BASE_URL=${AIAPI_URL}/v1/elevenlabs
ELEVEN_API_KEY=${AIAPI_KEY}
ELEVENLABS_API_KEY=${AIAPI_KEY}

# ============================================================================
# Tavily Configuration
# ============================================================================
TAVILY_API_BASE=${AIAPI_URL}/v1/tavily
TAVILY_BASE_URL=${AIAPI_URL}/v1/tavily
TAVILY_API_KEY=${AIAPI_KEY}

# ============================================================================
# SerpAPI Configuration
# ============================================================================
SERPAPI_BASE_URL=${AIAPI_URL}/v1/serpapi
SERPAPI_API_KEY=${AIAPI_KEY}

# ============================================================================
# Notes
# ============================================================================
# - Set AIAPI_URL and AIAPI_KEY before using this file
# - All API clients configured with these variables will use the proxy
# - The proxy handles authentication and forwards requests to real APIs
# - Usage tracking is automatic when using the proxy
# - WebSocket endpoints are also available for ElevenLabs TTS/STT
#
# WebSocket URLs:
# - TTS: ws://${AIAPI_URL}/v1/elevenlabs/text-to-speech/websocket
# - STT: ws://${AIAPI_URL}/v1/elevenlabs/speech-to-text/websocket
